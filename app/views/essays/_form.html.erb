<%= form_for(@essay) do |f| %>
	<% if @essay.errors.any? %>
		<div id="error_explanation">
			<h2><%= pluralize(@essay.errors.count, "error") %> prohibited this essay from being saved:</h2>

			<ul>
				<% @essay.errors.full_messages.each do |message| %>
					<li><%= message %></li>
				<% end %>
			</ul>
		</div>
	<% end %>

	<div class="field">

		<div class="paper"> <div class="paper-content"><%= f.text_area :body, spellcheck: "false", disabled: "true", id:"essay-submission-field" %></div></div>
		<br>
		<div class="actions center-align">
			<%= button_tag( :class => "btn") do %>
				Submit for Grading <i class="material-icons right">send</i>
			<% end %>		
		</div>
	<% end %>
