<%= form_for(@essay) do |f| %>
	<% if @essay.errors.any? %>
		<div id="error_explanation">
			<h2><%= pluralize(@essay.errors.count, "error") %> prohibited this essay from being saved:</h2>

			<ul>
				<% @essay.errors.full_messages.each do |message| %>
					<li><%= message %></li>
				<% end %>
			</ul>
		</div>
	<% end %>

	<div class="field">

		<div class="paper"> <div class="paper-content"><%= f.text_area :body, spellcheck: "false" %></div></div>
		<br>
		<div class="actions center-align">
			<%= f.submit "Submit Essay", class:"btn center-align" %>
		</div>
	<% end %>
